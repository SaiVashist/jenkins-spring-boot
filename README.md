# Simple Spring Boot Web App

Endpoints:
- `/` -> Hello message
- `/health` -> OK

## Run locally
```bash
mvn clean package
mvn spring-boot:run
```

## Build a jar
```bash
mvn clean package
java -jar target/demo-0.0.1-SNAPSHOT.jar
```
